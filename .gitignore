# Maintainer-only notes + local launcher scripts (never published)
.local/

# Codex CLI artifact (empty marker, created when .codex companion runs)
.codex

# Build output (local pipeline simulation only; CI builds fresh)
build/

# Local cache (transient, regenerated by crawler)
.cache/

# Generated artifacts (committed via workflow, not by humans)
# These are intentionally NOT gitignored — committed by daily-lint workflow
# to be served from gh-pages branch. The main branch keeps them empty.
# data/
# badges/
# site/dist/

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Node (worker)
worker/node_modules/
worker/dist/
worker/.wrangler/

# Local env
.env
.env.local

# OS junk
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
